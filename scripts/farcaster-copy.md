# Farcaster cast copy — USDC Directory launch

Open Warpcast → New Cast → paste. Post the URLs on their own lines so Warpcast renders them as rich preview cards.

---

## ⭐ Main launch cast (recommended)

Post in channel: **/ai-agents**

```
🤖 USDC Directory is now open for autonomous AI agent self-listing.

Any agent with a wallet can list itself for 5 USDC on Base, Ethereum, Arbitrum, OP, Polygon, Avalanche, BNB, Linea, Monad, Solana, Sui or Near.

Native x402 (gasless, EIP-3009) on Base.

https://usdc.directory/.well-known/x402
https://usdc.directory/api-docs
```

---

## Short variant for /base

```
🤖 Agents on Base — self-list on USDC Directory for 5 USDC.

Native x402, gasless EIP-3009. One POST and you're indexed.

https://usdc.directory/.well-known/x402
```

---

## Variant for /x402

```
New x402 endpoint live: agent self-listing for 5 USDC.

Manifest: https://usdc.directory/.well-known/x402
Treasury settles on Base. Pay-then-submit-tx fallback for every major chain.

If you're building an agent that needs distribution, this is one HTTP call.
```

---

## Thread version (3 casts for more reach)

### Cast 1 — hook
```
USDC Directory just became the first public directory where autonomous AI agents can list themselves on-chain.

5 USDC. Any chain. One POST.

🧵
```

### Cast 2 — how
```
Native x402 on Base (gasless EIP-3009 transferWithAuthorization).

For Ethereum, Arbitrum, OP, Polygon, Avalanche, BNB, Linea, Monad, Solana, Sui, Near: pay 5 USDC to treasury, submit tx hash, backend verifies on-chain, listing goes live.

Manifest 👇
https://usdc.directory/.well-known/x402
```

### Cast 3 — CTA
```
If your agent has a wallet, it has distribution.

Docs + curl examples → https://usdc.directory/api-docs
MCP endpoint for Claude/Cursor → connect "usdc-directory"

Cast a reply with your agent name and I'll boost it.
```

---

## Reply-bait single cast (best for organic engagement)

```
Question for /ai-agents builders:

If your agent could spend 5 USDC to get listed in a public directory crawled by GPT, Claude, and Perplexity — would it?

Mine can, starting today: https://usdc.directory/.well-known/x402

What would you list?
```

---

## Suggested channels (in priority order)

1. **/ai-agents** — primary audience
2. **/base** — agents-on-Base story
3. **/x402** — protocol-native crowd (small but high-signal)
4. **/coinbase** — Coinbase x402 team reads this
5. **/founders** — broader reach
6. **/dev** — fallback

Post the main cast in /ai-agents first. Wait ~30 min, then post the short variants in /base and /x402. Don't post the same text in 3 channels back-to-back — Warpcast throttles duplicates.

---

## Posting tips that actually move the needle

- **URLs on their own line** → Warpcast renders them as a frame/embed. Inline links don't.
- **Tag @dwr.eth, @v, @jessepollak** in a reply (not the main cast) if it gets >5 likes — they recast Base-aligned launches.
- **First reply matters** — leave the first reply yourself with a curl example or a screenshot of your /api-docs page.
- **Repost after 6 hours** with a different angle ("here's the first agent that self-listed") if you got <10 reactions.
- **Cross-post the same copy to X/Twitter** for double exposure — same text works on both.

---

## After you cast

Reply here with the Warpcast URL and I'll:
- Add it as a backlink on `/api-docs`
- Submit the cast hash to x402 Bazaar's GitHub issue
- Track impressions via Neynar's public cast endpoint (no key needed for read)
